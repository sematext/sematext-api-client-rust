/* 
 * Sematext Cloud API
 *
 * API Explorer provides access and documentation for Sematext REST API. The REST API requires the API Key to be sent as part of `Authorization` header. E.g.: `Authorization : apiKey e5f18450-205a-48eb-8589-7d49edaea813`.
 *
 * OpenAPI spec version: v3
 * 
 * Generated by: https://github.com/swagger-api/swagger-codegen.git
 */
#![allow(unused_imports)]
use std::sync::Arc;
use std::borrow::Borrow;
use std::borrow::Cow;
use std::collections::HashMap;

use hyper;
use serde_json;
use serde_json::Value;
use tokio::runtime::Runtime;
use futures;
use futures::{Future, Stream};
use bigdecimal::BigDecimal;

use hyper::Body;
use hyper::body::Bytes;
use hyper::body::HttpBody;
use std::str::FromStr;
use chrono::{Date, NaiveDateTime, DateTime, FixedOffset, Utc, SecondsFormat};
use crate::{OutlinePrint};
use crate::models::*;
use super::{Error, configuration};
use headers::{Authorization, Header};
use headers::authorization::Credentials;

pub struct TagApiControllerApiClient<C: hyper::client::connect::Connect + Clone + Send + Sync> {
    configuration: Arc<configuration::Configuration<C>>,
}

impl<C: hyper::client::connect::Connect + Clone + Send + Sync + 'static> TagApiControllerApiClient<C> {
    pub fn new(configuration: Arc<configuration::Configuration<C>>) -> TagApiControllerApiClient<C> {
        TagApiControllerApiClient {
            configuration: configuration,
        }
    }
}

#[async_trait::async_trait]
pub trait TagApiControllerApi {
    async fn get_tag_names_using_get(&self, app_ids: &str, from: i64, to: i64, metrics: bool, logs: bool, events: bool, rum: bool) -> Result<TagNamesResponse, Error<serde_json::Value>>;
    async fn get_tags_grouped_by_id_using_get1(&self, app_ids: &str, tag: Vec<String>, from: i64, to: i64, metrics: bool, logs: bool, events: bool, rum: bool) -> Result<TagsGroupedResponse, Error<serde_json::Value>>;
    async fn get_using_get(&self, app_ids: &str, tag: Vec<String>, from: i64, to: i64, metrics: bool, logs: bool, events: bool, rum: bool) -> Result<::std::collections::HashMap<String, Dimension>, Error<serde_json::Value>>;
    async fn get_using_get1(&self, app_ids: &str, tag: Vec<String>, from: i64, to: i64, metrics: bool, logs: bool, events: bool, rum: bool) -> Result<::std::collections::HashMap<String, Dimension>, Error<serde_json::Value>>;
}

#[async_trait::async_trait]
impl<C: hyper::client::connect::Connect + Clone + Send + Sync + 'static>TagApiControllerApi for TagApiControllerApiClient<C> {
 ///
 /// Gets tag names for the given application identifiers appearing in the given time frame.
 ///
 /// 
 ///
 /// # Arguments
 ///
 /// * `app_ids` - Gets tag names for the given application identifiers appearing in the given time frame.
 /// 
 ///
 /// * `from` - Gets tag names for the given application identifiers appearing in the given time frame.
 /// 
 ///
 /// * `to` - Gets tag names for the given application identifiers appearing in the given time frame.
 /// 
 ///
 /// * `metrics` - Gets tag names for the given application identifiers appearing in the given time frame.
 /// 
 ///
 /// * `logs` - Gets tag names for the given application identifiers appearing in the given time frame.
 /// 
 ///
 /// * `events` - Gets tag names for the given application identifiers appearing in the given time frame.
 /// 
 ///
 /// * `rum` - Gets tag names for the given application identifiers appearing in the given time frame.
 /// 
 ///
    async fn get_tag_names_using_get(&self, app_ids: &str, from: i64, to: i64, metrics: bool, logs: bool, events: bool, rum: bool) -> Result<TagNamesResponse, Error<serde_json::Value>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let mut auth_headers = HashMap::<String, String>::new();
        let mut auth_query = HashMap::<String, String>::new();
        if let Some(ref apikey) = configuration.api_key {
            let key = apikey.key.clone();
            let val = match apikey.prefix {
                Some(ref prefix) => format!("{} {}", prefix, key),
                None => key,
            };
            auth_headers.insert("Authorization".to_owned(), val);
        };
        let method = hyper::Method::GET;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            let has_query_params = true;
            query.append_pair("from", &from.outline_print() );
            query.append_pair("to", &to.outline_print() );
            query.append_pair("metrics", &metrics.outline_print() );
            query.append_pair("logs", &logs.outline_print() );
            query.append_pair("events", &events.outline_print() );
            query.append_pair("rum", &rum.outline_print() );
            for (key, val) in &auth_query {
                query.append_pair(key, val);
            }
            if has_query_params || auth_query.len()>0  {
                format!("/?{}", query.finish())
            } else {
                "".to_string()
            }
        };
        let uri_str = format!("{}spm-reports/api/v3/apps/{appIds}/tagNames{}", configuration.base_path, query_string, appIds=app_ids);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        //dbg!(&uri_str);

        let uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req =
            hyper::Request::builder()
                .method(method)
                .uri(uri);

        let headers = req.headers_mut().unwrap();

        if let Some(ref user_agent) = configuration.user_agent {
            headers.insert(hyper::header::USER_AGENT, user_agent.parse().unwrap());
        }


        for (key, val) in auth_headers {
            headers.insert(
                hyper::header::HeaderName::from_str(key.as_ref()).unwrap(),
                val.parse().unwrap(),
            );
        }

        let somebody = Body::empty();

        let req = req.body(somebody).unwrap();

        let res = configuration
            .client.request(req)
            .await
            .map_err(|e| -> Error<serde_json::Value> { Error::from(e) });

        let mut res = res?;

        let status = res.status();
        let mut res_body: Vec<u8> = vec![];

        while let Some(chunk) = res.body_mut().data().await {
            let mut chunk_vec = chunk.unwrap().to_vec();
            res_body.append(chunk_vec.as_mut());
        }

        //Uncomment to see what went wrong
/*
        let string_result = std::str::from_utf8(&res_body).unwrap();
        let value_result: Result<serde_json::Value, serde_json::Error> = serde_json::from_str(&string_result);
        if let Ok(json_value) = value_result {
            //Valid json, invalid structure, pretty-printed output
            eprintln!("{}", serde_json::to_string_pretty(&json_value).unwrap());
        } else {
            //Invalid json, raw output
            dbg!(&string_result);
        }
*/
        let res_body =
            if status.is_success() {
                Ok(res_body)
            } else {
                Err(Error::from((status, res_body.borrow())))
            };

        let mut res_body = res_body?;

        let res_body =
            serde_json::from_slice(res_body.borrow())
            .map_err(|e| -> Error<serde_json::Value> { Error::from(e) });

        res_body
    }

 ///
 /// Gets tag names grouped by application id.
 ///
 /// 
 ///
 /// # Arguments
 ///
 /// * `app_ids` - Gets tag names grouped by application id.
 /// 
 ///
 /// * `tag` - Gets tag names grouped by application id.
 /// 
 ///
 /// * `from` - Gets tag names grouped by application id.
 /// 
 ///
 /// * `to` - Gets tag names grouped by application id.
 /// 
 ///
 /// * `metrics` - Gets tag names grouped by application id.
 /// 
 ///
 /// * `logs` - Gets tag names grouped by application id.
 /// 
 ///
 /// * `events` - Gets tag names grouped by application id.
 /// 
 ///
 /// * `rum` - Gets tag names grouped by application id.
 /// 
 ///
    async fn get_tags_grouped_by_id_using_get1(&self, app_ids: &str, tag: Vec<String>, from: i64, to: i64, metrics: bool, logs: bool, events: bool, rum: bool) -> Result<TagsGroupedResponse, Error<serde_json::Value>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let mut auth_headers = HashMap::<String, String>::new();
        let mut auth_query = HashMap::<String, String>::new();
        if let Some(ref apikey) = configuration.api_key {
            let key = apikey.key.clone();
            let val = match apikey.prefix {
                Some(ref prefix) => format!("{} {}", prefix, key),
                None => key,
            };
            auth_headers.insert("Authorization".to_owned(), val);
        };
        let method = hyper::Method::GET;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            let has_query_params = true;
            query.append_pair("from", &from.outline_print() );
            query.append_pair("to", &to.outline_print() );
            for p in tag.iter() {
                query.append_pair("tag", &p.to_string());
            }
            query.append_pair("metrics", &metrics.outline_print() );
            query.append_pair("logs", &logs.outline_print() );
            query.append_pair("events", &events.outline_print() );
            query.append_pair("rum", &rum.outline_print() );
            for (key, val) in &auth_query {
                query.append_pair(key, val);
            }
            if has_query_params || auth_query.len()>0  {
                format!("/?{}", query.finish())
            } else {
                "".to_string()
            }
        };
        let uri_str = format!("{}spm-reports/api/v3/apps/{appIds}/grouped{}", configuration.base_path, query_string, appIds=app_ids);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        //dbg!(&uri_str);

        let uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req =
            hyper::Request::builder()
                .method(method)
                .uri(uri);

        let headers = req.headers_mut().unwrap();

        if let Some(ref user_agent) = configuration.user_agent {
            headers.insert(hyper::header::USER_AGENT, user_agent.parse().unwrap());
        }


        for (key, val) in auth_headers {
            headers.insert(
                hyper::header::HeaderName::from_str(key.as_ref()).unwrap(),
                val.parse().unwrap(),
            );
        }

        let somebody = Body::empty();

        let req = req.body(somebody).unwrap();

        let res = configuration
            .client.request(req)
            .await
            .map_err(|e| -> Error<serde_json::Value> { Error::from(e) });

        let mut res = res?;

        let status = res.status();
        let mut res_body: Vec<u8> = vec![];

        while let Some(chunk) = res.body_mut().data().await {
            let mut chunk_vec = chunk.unwrap().to_vec();
            res_body.append(chunk_vec.as_mut());
        }

        //Uncomment to see what went wrong
/*
        let string_result = std::str::from_utf8(&res_body).unwrap();
        let value_result: Result<serde_json::Value, serde_json::Error> = serde_json::from_str(&string_result);
        if let Ok(json_value) = value_result {
            //Valid json, invalid structure, pretty-printed output
            eprintln!("{}", serde_json::to_string_pretty(&json_value).unwrap());
        } else {
            //Invalid json, raw output
            dbg!(&string_result);
        }
*/
        let res_body =
            if status.is_success() {
                Ok(res_body)
            } else {
                Err(Error::from((status, res_body.borrow())))
            };

        let mut res_body = res_body?;

        let res_body =
            serde_json::from_slice(res_body.borrow())
            .map_err(|e| -> Error<serde_json::Value> { Error::from(e) });

        res_body
    }

 ///
 /// Gets values for specified tags for the given application identifiers appearing in the given time frame.
 ///
 /// 
 ///
 /// # Arguments
 ///
 /// * `app_ids` - Gets values for specified tags for the given application identifiers appearing in the given time frame.
 /// 
 ///
 /// * `tag` - Gets values for specified tags for the given application identifiers appearing in the given time frame.
 /// 
 ///
 /// * `from` - Gets values for specified tags for the given application identifiers appearing in the given time frame.
 /// 
 ///
 /// * `to` - Gets values for specified tags for the given application identifiers appearing in the given time frame.
 /// 
 ///
 /// * `metrics` - Gets values for specified tags for the given application identifiers appearing in the given time frame.
 /// 
 ///
 /// * `logs` - Gets values for specified tags for the given application identifiers appearing in the given time frame.
 /// 
 ///
 /// * `events` - Gets values for specified tags for the given application identifiers appearing in the given time frame.
 /// 
 ///
 /// * `rum` - Gets values for specified tags for the given application identifiers appearing in the given time frame.
 /// 
 ///
    async fn get_using_get(&self, app_ids: &str, tag: Vec<String>, from: i64, to: i64, metrics: bool, logs: bool, events: bool, rum: bool) -> Result<::std::collections::HashMap<String, Dimension>, Error<serde_json::Value>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let mut auth_headers = HashMap::<String, String>::new();
        let mut auth_query = HashMap::<String, String>::new();
        if let Some(ref apikey) = configuration.api_key {
            let key = apikey.key.clone();
            let val = match apikey.prefix {
                Some(ref prefix) => format!("{} {}", prefix, key),
                None => key,
            };
            auth_headers.insert("Authorization".to_owned(), val);
        };
        let method = hyper::Method::GET;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            let has_query_params = true;
            query.append_pair("from", &from.outline_print() );
            query.append_pair("to", &to.outline_print() );
            for p in tag.iter() {
                query.append_pair("tag", &p.to_string());
            }
            query.append_pair("metrics", &metrics.outline_print() );
            query.append_pair("logs", &logs.outline_print() );
            query.append_pair("events", &events.outline_print() );
            query.append_pair("rum", &rum.outline_print() );
            for (key, val) in &auth_query {
                query.append_pair(key, val);
            }
            if has_query_params || auth_query.len()>0  {
                format!("/?{}", query.finish())
            } else {
                "".to_string()
            }
        };
        let uri_str = format!("{}spm-reports/api/v3/apps/{appIds}/metrics/filters{}", configuration.base_path, query_string, appIds=app_ids);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        //dbg!(&uri_str);

        let uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req =
            hyper::Request::builder()
                .method(method)
                .uri(uri);

        let headers = req.headers_mut().unwrap();

        if let Some(ref user_agent) = configuration.user_agent {
            headers.insert(hyper::header::USER_AGENT, user_agent.parse().unwrap());
        }


        for (key, val) in auth_headers {
            headers.insert(
                hyper::header::HeaderName::from_str(key.as_ref()).unwrap(),
                val.parse().unwrap(),
            );
        }

        let somebody = Body::empty();

        let req = req.body(somebody).unwrap();

        let res = configuration
            .client.request(req)
            .await
            .map_err(|e| -> Error<serde_json::Value> { Error::from(e) });

        let mut res = res?;

        let status = res.status();
        let mut res_body: Vec<u8> = vec![];

        while let Some(chunk) = res.body_mut().data().await {
            let mut chunk_vec = chunk.unwrap().to_vec();
            res_body.append(chunk_vec.as_mut());
        }

        //Uncomment to see what went wrong
/*
        let string_result = std::str::from_utf8(&res_body).unwrap();
        let value_result: Result<serde_json::Value, serde_json::Error> = serde_json::from_str(&string_result);
        if let Ok(json_value) = value_result {
            //Valid json, invalid structure, pretty-printed output
            eprintln!("{}", serde_json::to_string_pretty(&json_value).unwrap());
        } else {
            //Invalid json, raw output
            dbg!(&string_result);
        }
*/
        let res_body =
            if status.is_success() {
                Ok(res_body)
            } else {
                Err(Error::from((status, res_body.borrow())))
            };

        let mut res_body = res_body?;

        let res_body =
            serde_json::from_slice(res_body.borrow())
            .map_err(|e| -> Error<serde_json::Value> { Error::from(e) });

        res_body
    }

 ///
 /// Gets values for specified tags for the given application identifiers appearing in the given time frame.
 ///
 /// 
 ///
 /// # Arguments
 ///
 /// * `app_ids` - Gets values for specified tags for the given application identifiers appearing in the given time frame.
 /// 
 ///
 /// * `tag` - Gets values for specified tags for the given application identifiers appearing in the given time frame.
 /// 
 ///
 /// * `from` - Gets values for specified tags for the given application identifiers appearing in the given time frame.
 /// 
 ///
 /// * `to` - Gets values for specified tags for the given application identifiers appearing in the given time frame.
 /// 
 ///
 /// * `metrics` - Gets values for specified tags for the given application identifiers appearing in the given time frame.
 /// 
 ///
 /// * `logs` - Gets values for specified tags for the given application identifiers appearing in the given time frame.
 /// 
 ///
 /// * `events` - Gets values for specified tags for the given application identifiers appearing in the given time frame.
 /// 
 ///
 /// * `rum` - Gets values for specified tags for the given application identifiers appearing in the given time frame.
 /// 
 ///
    async fn get_using_get1(&self, app_ids: &str, tag: Vec<String>, from: i64, to: i64, metrics: bool, logs: bool, events: bool, rum: bool) -> Result<::std::collections::HashMap<String, Dimension>, Error<serde_json::Value>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let mut auth_headers = HashMap::<String, String>::new();
        let mut auth_query = HashMap::<String, String>::new();
        if let Some(ref apikey) = configuration.api_key {
            let key = apikey.key.clone();
            let val = match apikey.prefix {
                Some(ref prefix) => format!("{} {}", prefix, key),
                None => key,
            };
            auth_headers.insert("Authorization".to_owned(), val);
        };
        let method = hyper::Method::GET;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            let has_query_params = true;
            query.append_pair("from", &from.outline_print() );
            query.append_pair("to", &to.outline_print() );
            for p in tag.iter() {
                query.append_pair("tag", &p.to_string());
            }
            query.append_pair("metrics", &metrics.outline_print() );
            query.append_pair("logs", &logs.outline_print() );
            query.append_pair("events", &events.outline_print() );
            query.append_pair("rum", &rum.outline_print() );
            for (key, val) in &auth_query {
                query.append_pair(key, val);
            }
            if has_query_params || auth_query.len()>0  {
                format!("/?{}", query.finish())
            } else {
                "".to_string()
            }
        };
        let uri_str = format!("{}spm-reports/api/v3/apps/{appIds}/tags{}", configuration.base_path, query_string, appIds=app_ids);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        //dbg!(&uri_str);

        let uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req =
            hyper::Request::builder()
                .method(method)
                .uri(uri);

        let headers = req.headers_mut().unwrap();

        if let Some(ref user_agent) = configuration.user_agent {
            headers.insert(hyper::header::USER_AGENT, user_agent.parse().unwrap());
        }


        for (key, val) in auth_headers {
            headers.insert(
                hyper::header::HeaderName::from_str(key.as_ref()).unwrap(),
                val.parse().unwrap(),
            );
        }

        let somebody = Body::empty();

        let req = req.body(somebody).unwrap();

        let res = configuration
            .client.request(req)
            .await
            .map_err(|e| -> Error<serde_json::Value> { Error::from(e) });

        let mut res = res?;

        let status = res.status();
        let mut res_body: Vec<u8> = vec![];

        while let Some(chunk) = res.body_mut().data().await {
            let mut chunk_vec = chunk.unwrap().to_vec();
            res_body.append(chunk_vec.as_mut());
        }

        //Uncomment to see what went wrong
/*
        let string_result = std::str::from_utf8(&res_body).unwrap();
        let value_result: Result<serde_json::Value, serde_json::Error> = serde_json::from_str(&string_result);
        if let Ok(json_value) = value_result {
            //Valid json, invalid structure, pretty-printed output
            eprintln!("{}", serde_json::to_string_pretty(&json_value).unwrap());
        } else {
            //Invalid json, raw output
            dbg!(&string_result);
        }
*/
        let res_body =
            if status.is_success() {
                Ok(res_body)
            } else {
                Err(Error::from((status, res_body.borrow())))
            };

        let mut res_body = res_body?;

        let res_body =
            serde_json::from_slice(res_body.borrow())
            .map_err(|e| -> Error<serde_json::Value> { Error::from(e) });

        res_body
    }

}
